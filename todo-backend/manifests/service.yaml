apiVersion: v1
kind: Service
metadata:
  name: todo-backend-svc
  namespace: project
  labels:
    app: todo-backend-app
spec:
  type: ClusterIP
  selector:
    app: todo-backend-app
  ports:
    - protocol: TCP
      port: 3000