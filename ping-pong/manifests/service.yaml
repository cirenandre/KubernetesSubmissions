apiVersion: v1
kind: Service
metadata:
  name: ping-pong-svc
  namespace: exercises
  labels:
    app: ping-pong-app
spec:
  type: ClusterIP
  selector:
    app: ping-pong-app
  ports:
    - protocol: TCP
      port: 3000
