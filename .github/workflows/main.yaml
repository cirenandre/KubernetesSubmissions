name: Release application

on:
  push:
    branches:
      - '**'

permissions:
  contents: write
  id-token: write

env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}
  GKE_CLUSTER: dwk-cluster
  GKE_ZONE: europe-north1-b
  REGISTRY: europe-north1-docker.pkg.dev
  REPOSITORY: dwk-repo
  BRANCH: ${{ github.ref_name }}

jobs:
  build-publish-deploy:
    name: Build, Publish and Deploy
    runs-on: ubuntu-latest
    environment: GKE_PROJECT

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: "${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}"
          service_account: "${{ secrets.SERVICE_ACCOUNT }}"

      - name: 'Set up Cloud SDK'
        uses: google-github-actions/setup-gcloud@v3

      - run: gcloud --quiet auth configure-docker $REGISTRY

      - name: 'Get GKE credentials'
        if: github.ref_name != 'main'
        uses: 'google-github-actions/get-gke-credentials@v3'
        with:
          cluster_name: '${{ env.GKE_CLUSTER }}'
          project_id: '${{ env.PROJECT_ID }}'
          location: '${{ env.GKE_ZONE }}'

      - name: 'Form the image names'
        run: |
          echo "IMAGE_TAG_APP=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app:$BRANCH-$GITHUB_SHA" >> $GITHUB_ENV
          echo "IMAGE_TAG_BACKEND=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-backend:$BRANCH-$GITHUB_SHA" >> $GITHUB_ENV
          echo "IMAGE_TAG_CRON=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-cron:$BRANCH-$GITHUB_SHA" >> $GITHUB_ENV
          echo "IMAGE_TAG_BROADCASTER=$REGISTRY/$PROJECT_ID/$REPOSITORY/broadcaster:$BRANCH-$GITHUB_SHA" >> $GITHUB_ENV

      - name: Build todo-app
        run: docker build --tag $IMAGE_TAG_APP ./todo-app

      - name: Build todo-backend
        run: docker build --tag $IMAGE_TAG_BACKEND ./todo-backend

      - name: Build todo-cron
        run: docker build --tag $IMAGE_TAG_CRON ./todo-cron

      - name: Build broadcaster
        run: docker build --tag $IMAGE_TAG_BROADCASTER ./broadcaster

      - name: Publish todo-app
        run: docker push $IMAGE_TAG_APP

      - name: Publish todo-backend
        run: docker push $IMAGE_TAG_BACKEND

      - name: Publish todo-cron
        run: docker push $IMAGE_TAG_CRON

      - name: Publish broadcaster
        run: docker push $IMAGE_TAG_BROADCASTER

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v3

      - name: 'Deploy to branch namespace (feature branches only)'
        if: github.ref_name != 'main'
        working-directory: project/manifests/overlays/gke
        run: |-
          kubectl create namespace $BRANCH || true
          kubectl config set-context --current --namespace=$BRANCH
          kustomize edit set namespace $BRANCH
          kustomize edit set image todo-app=$IMAGE_TAG_APP
          kustomize edit set image todo-backend-app=$IMAGE_TAG_BACKEND
          kustomize edit set image todo-cron=$IMAGE_TAG_CRON
          kustomize edit set image broadcaster=$IMAGE_TAG_BROADCASTER
          kustomize build . | kubectl apply -f -
          kubectl rollout status deployment/todo-app -n $BRANCH
          kubectl rollout status deployment/todo-backend-app -n $BRANCH
          kubectl rollout status deployment/broadcaster -n $BRANCH
          kubectl get services -n $BRANCH -o wide

      # GitOps: main is not deployed directly here. Instead, the image tags
      # are committed back to the repo, and ArgoCD (watching this path on
      # main) picks up the commit and syncs the cluster automatically.
      - name: 'Update image tags for GitOps (main only)'
        if: github.ref_name == 'main'
        working-directory: project/manifests/overlays/gke
        run: |-
          kustomize edit set image todo-app=$IMAGE_TAG_APP
          kustomize edit set image todo-backend-app=$IMAGE_TAG_BACKEND
          kustomize edit set image todo-cron=$IMAGE_TAG_CRON
          kustomize edit set image broadcaster=$IMAGE_TAG_BROADCASTER

      - name: 'Commit kustomization.yaml to GitHub (main only)'
        if: github.ref_name == 'main'
        uses: EndBug/add-and-commit@v10
        with:
          add: 'project/manifests/overlays/gke/kustomization.yaml'
          message: "New project version released ${{ github.sha }}"
