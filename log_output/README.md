# log_output

Exercise 1.1 of the DevOps with Kubernetes course.

Outputs a random UUID, generated once when the application starts, together
with the current timestamp. A new line is logged to the console every 5
seconds.

## Running

Requires Node.js (no external dependencies).

```bash
node app.js
```

or

```bash
npm start
```
